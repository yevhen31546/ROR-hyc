= semantic_form_for [:admin, @product_category] do |f|
  = render 'layouts/error_messages', :target => object
  %ul
    = f.input :name
    = f.input :position
    = f.commit_button (f.object.new_record? ? 'Add product category' : 'Edit product category')
